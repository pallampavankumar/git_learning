# Undo Changes Using git restore

## What is git restore?
`git restore` is used to discard changes in the working directory or staging area.

It helps revert files to the last committed state.

---

## Undo Changes in Working Directory

```bash
git restore file.txt
