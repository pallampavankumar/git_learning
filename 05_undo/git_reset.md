# git reset (Soft, Mixed, Hard)

## What is git reset?
`git reset` is used to move the current branch pointer (HEAD) to a previous commit.

It can also affect:
- Staging area
- Working directory

---

## Types of git reset

### 1️⃣ --soft
```bash
git reset --soft HEAD~1
