# git add and git commit

## Purpose of git add
`git add` moves changes from the working directory to the staging area.

It allows you to decide exactly what should go into the next commit.

---

## Purpose of git commit
`git commit` records a snapshot of staged files into the repository.

Each commit has:
- A unique hash
- Author information
- Commit message
- Timestamp

---