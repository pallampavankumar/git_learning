# git clone and git fetch

## What is git clone?
`git clone` creates a full copy of a remote repository on your local machine.

It includes:
- All files
- Full commit history
- Branches


---

## Clone a Repository
```bash
git clone <repo-url>

##What is git fetch?
->git fetch downloads changes from the remote repository
->but does NOT merge them into your current branch.